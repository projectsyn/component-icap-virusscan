apiVersion: v1
kind: Service
metadata:
  labels:
    app: clamav-icap
    instance: defaults
  name: icap
  namespace: defaults
spec:
  ports:
    - name: icap
      port: 80
      protocol: TCP
      targetPort: icap
  selector:
    app: clamav-icap
    instance: defaults
  type: ClusterIP
