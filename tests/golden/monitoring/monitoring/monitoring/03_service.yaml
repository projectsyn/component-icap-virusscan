apiVersion: v1
kind: Service
metadata:
  labels:
    app: clamav-icap
    instance: monitoring
  name: icap
  namespace: monitoring
spec:
  ports:
    - name: icap
      port: 80
      protocol: TCP
      targetPort: icap
  selector:
    app: clamav-icap
    instance: monitoring
  type: ClusterIP
