apiVersion: v1
kind: Service
metadata:
  labels:
    app: clamav-icap
    instance: with_squid_proxy
  name: icap
  namespace: syn-icap-virusscan
spec:
  ports:
    - name: icap
      port: 80
      protocol: TCP
      targetPort: icap
  selector:
    app: clamav-icap
    instance: with_squid_proxy
  type: ClusterIP
